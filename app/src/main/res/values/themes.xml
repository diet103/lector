<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark Material base so the cold-start window never flashes white; Compose takes over from there -->
    <style name="Theme.Lector" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <!-- ReadAloudActivity: a see-through window over the calling app; the scrim is Compose-drawn,
         so the platform dim is off. -->
    <style name="Theme.Lector.Transparent" parent="Theme.Lector">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
