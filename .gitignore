# Gradle
.gradle/
build/
app/build/

# Local machine config — never commit
local.properties
dev.properties

# Signing — never commit. keystore.properties holds the store/key passwords in plain text, so it is
# every bit as sensitive as the keystore itself.
*.keystore
*.jks
keystore.properties

# IDE
.idea/
*.iml
.kotlin/

# OS noise
.DS_Store
